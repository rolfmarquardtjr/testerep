'use client'

import { DashboardLayout } from '@/components/layout'

export default function ProfissionalPerfilLayout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout>{children}</DashboardLayout>
}
